# Python 3.11. Wersje przypięte tam, gdzie zmiana major realnie potrafi zepsuć
# działający deploy (FastAPI/uvicorn/httpx), i luźne tam, gdzie chcemy poprawki
# bezpieczeństwa bez ręcznego bumpa (psycopg2, anthropic).
#
# Lista jest KRÓTKA celowo. Każda paczka to coś, co może się nie zainstalować na
# świeżym VPS-ie o 2 w nocy, a ten system ma jedno zadanie: dowieźć zlecenie na
# telefon operatora w kilka minut.

fastapi==0.115.0
uvicorn==0.30.6          # serwer dla api/main.py (PM2 -> scripts/start_api.sh)
httpx==0.27.2            # wołania do Apify; obsługuje proxy per klucz (workers/apify_proxy.py)
psycopg2-binary>=2.9.9   # PostgreSQL; -binary, żeby nie wymagać kompilatora na VPS-ie
anthropic>=0.40.0        # klasyfikator postów (LLM_PROVIDER=anthropic, domyślny)
python-dotenv>=1.0.0     # .env — jedyne źródło konfiguracji
pytest>=8.0.0            # testy offline (laweta_radar/tests/)

# --- OPCJONALNE: pozostali dostawcy modelu (services/llm.py) -----------------
# Świadomie NIE są tu odkomentowane. Instalujesz je tylko wtedy, gdy realnie
# przełączasz LLM_PROVIDER albo porównujesz modele na własnych danych
# (scripts/porownaj_modele.py). Brak paczki daje CZYTELNY komunikat przy
# starcie (`python -m laweta_radar.services.llm`), a nie ImportError w środku
# runu — dlatego import każdego SDK jest leniwy i lista może zostać krótka.
# openai>=1.40.0         # LLM_PROVIDER=openai
# google-genai>=1.0.0    # LLM_PROVIDER=gemini

# --- OPCJONALNE: web push z panelu (services/powiadomienia._wyslij_push) -----
# Ta sama zasada co wyżej. Szyfrowanie treści push ciągnie `cryptography` —
# największą paczkę w całym projekcie, z kompilacją na części VPS-ów. Deploy
# korzystający wyłącznie z Telegrama (czyli domyślny) nie ma powodu jej budować:
# pywebpush>=2.0.0       # web push (VAPID); bez niej push po prostu milczy

# socks5:// w APIFY_PROXY_* wymaga dodatkowo: pip install "httpx[socks]"
# Dla http:// i https:// nic nie trzeba dokładać — patrz docs/APIFY-PROXY.md.
